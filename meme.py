class Meme(object):

  def __init__(self, image, first, last, font='default', color='000000'):
    self.image = image
    self.first = first
    self.last = last
    self.font = font
    self.color = color
#    self.image = self.validate_image(image)
#    self.first = self.validate_xy(first)
#    self.last = self.validate_xy(last)
#    self.font = self.validate_font(font)
#    self.color = self.validate_color(color)

  

